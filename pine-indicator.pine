//@version=limited
indicator("5m + 15m Automatic Levels")

// Exact automatic drawing system for the current 5m/15m buckets.
// The runtime supplies exact bucket opens from Binance 1m candles.
exactDrawings()
