//@version=limited
indicator("5m + 15m Automatic Levels")

// This project supports the custom exactDrawings() primitive so the
// original 5m/15m lines + box can run through the Pine-compatible system.
exactDrawings()
